# tests/ — Automated proof the maths is correct

| File | What it does |
|------|----------------|
| `test_checks.py` | Unit tests per check + **official sample** must match the brief |

**Run:** `pytest tests/ -v`  
If `TestSampleVerification` passes, the tool matches the assessment spreadsheet.
