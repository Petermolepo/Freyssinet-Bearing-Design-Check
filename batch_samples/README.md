# batch_samples/ — Example CSV for batch mode

| File | What it does |
|------|----------------|
| `sample_batch.csv` | Four example bearings (one row each) |

**Run:** `python main.py --batch batch_samples/sample_batch.csv`  
Optional `label` column names each row in the summary table.
