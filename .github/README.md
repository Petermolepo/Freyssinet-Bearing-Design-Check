# .github/ — Automation

| Path | Purpose |
|------|---------|
| `workflows/ci.yml` | Runs on every push: lint → tests → verification sample → feature smoke |

Four stages prove code quality and that the official sample still matches the brief.
