# presentation/ — Interview / demo slide deck

| File | What it does |
|------|----------------|
| `index.html` | Full-screen HTML presentation (architecture, checks, demo flow) |

**Open:** `python main.py --web` then visit http://localhost:8000/presentation  
Or open `index.html` directly in a browser (some links work best via the server).
