# scripts/ — CI helper scripts

| File | What it does |
|------|----------------|
| `verify_sample.py` | Stage 3 CI — runs only the verification sample tests |
| `smoke_features.py` | Stage 4 CI — batch, exports, JSON, CLI exit codes |

Not needed for day-to-day use; GitHub Actions calls these.
